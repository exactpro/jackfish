#TestCase;#Kind;#Depends;#For
Dialog get properties;Never;;
    #Id;#Step;#Kind;#For;#Depends
    Start;;;;
        #Id;#Global;#Action;MainClass;Args;Jar;$AppId
        APPSTR3;1;ApplicationStart;mockJavaFxMainClass;'';mockFXJar;mockFXApp

    #EndStep

    #Id;#Step;#Kind;#For;#Depends
    Positive;;;;
        #Id;#Action;#Assert;$Size;$Position;$Dialog;$AppConnection
        APPPAR2;DialogGetProperties;This.Result == Result.Passed && This.Out.Size != null && This.Out.Position != null;;;'Dialog';APPSTR3.Out

        #Id;#Action;#Assert;$Size;$Dialog;$AppConnection
        APPPAR3;DialogGetProperties;This.Result == Result.Passed && This.Out.Size != null;;'Dialog';APPSTR3.Out

        #Id;#Action;#Assert;$Position;$Dialog;$AppConnection
        APPPAR4;DialogGetProperties;This.Result == Result.Passed && This.Out.Position != null;;'Dialog';APPSTR3.Out

        #Id;#Action;#Assert;$Dialog;$AppConnection
        APPPAR5;DialogGetProperties;This.Result == Result.Passed && This.Out.size == 0;'Dialog';APPSTR3.Out

    #EndStep

    #Id;#Step;#Kind;#For;#Depends
    Negative;;;;
        #Id;#Action;#Assert;$Dialog;$AppConnection
        APPPAR7;DialogGetProperties;This.Result == Result.Failed && This.Kind == ErrorKind.DIALOG_NOT_FOUND;'Dialogg';APPSTR3.Out

        #Id;#Action;#Assert;$Dialog;$AppConnection
        APPPAR8;DialogGetProperties;This.Result == Result.Failed && This.Kind == ErrorKind.EMPTY_PARAMETER;'Dialog';

        #Id;#Action;#Assert;$Dialog;$AppConnection
        APPPAR9;DialogGetProperties;This.Result == Result.Failed && This.Kind == ErrorKind.EMPTY_PARAMETER;;APPSTR3.Out

        #Id;#Action;#Assert;$Dialog;$AppConnection
        APPPAR10;DialogGetProperties;This.Result == Result.Failed && This.Kind == ErrorKind.EXPRESSION_ERROR;'Dialog';asd

    #EndStep

    #Id;#Step;#Kind;#For;#Depends
    Stop;;;;
        #Action;$AppConnection
        ApplicationStop;APPSTR3.Out

    #EndStep


